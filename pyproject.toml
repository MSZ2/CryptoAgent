[project]
name = "cryptoagent"
entry = "app.main:app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crewai[google-genai]>=1.9.3",
]
